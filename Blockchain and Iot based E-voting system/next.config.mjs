/** @type {import('next').NextConfig} */
const nextConfig = {
  //   async redirects() {
  //     return [
  //       {
  //         source: "/((?!homepage).*)", // Match all routes
  //         destination: "/homepage", // Redirect to `/homepage`
  //         permanent: false, // Temporary redirect (use `true` for permanent)
  //       },
  //     ];
  //   },
};

export default nextConfig;
